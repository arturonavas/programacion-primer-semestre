Algoritmo T02_20
    Definir d1, d2, l Como Real;

    Escribir "primera diagonal: ";
    Leer d1;
    Escribir "segunda diagonal: ";
    Leer d2;

    l <- raiz((d1 / 2) * (d1 / 2) + (d2 / 2) * (d2 / 2));

    Escribir "Lado del rombo: ", l;
FinAlgoritmo
